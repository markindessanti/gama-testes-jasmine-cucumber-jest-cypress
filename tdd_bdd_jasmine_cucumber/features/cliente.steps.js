const assert = require('assert');
const {Given, When, Then} = require('cucumber');

Given('I\'m \{int} clients in my database', function (qtd) {
	return 'pending';
});

When('I access my homepage', function () {
	return 'pending';
});

Then('See the list of \{int} itens', function (qtd) {
	return 'pending';
});